import { deepEqual, ok } from 'assert';

import 'mocha';

describe('Suite de teste para exames', () =>
{
	it('Deve criar exames', async (): Promise<any> =>
	{

	});

	it('Deve listar exames', async (): Promise<any> =>
	{

	});

	it('Deve Atualizar um exame', async (): Promise<any> =>
	{

	});

	it('Deve remover um exame', async (): Promise<any> =>
	{
		
	});
});
